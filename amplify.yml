version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
        - npm install -g typescript
    build:
      commands:
        - tsc && vite build
  artifacts:
    baseDirectory: dist
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
      - .vite/**/*